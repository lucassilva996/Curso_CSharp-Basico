using System;
/// <summary>
/// Curso C# - Módulo Básico
/// Aula 12 - Arrays
/// Exemplo - 02 - Percorrer um array
/// </summary>
/// 

namespace Arrays02
{
    class Program
    {
        static void Main(string[] args)
        {
            //como percorrer um array
            // char[] vogais = new char[5] { 'a', 'e', 'i', 'o', 'u' };
            //for (int i = 0; i <= 4; i++)
            //    Console.WriteLine(vogais[i]);
            //Console.ReadKey();

            //int[] numeros = new int[30];

            //for (int i = 0; i <= numeros.Length - 1; i++)
            //    numeros[i] = i;

            //for (int i = 0; i <= numeros.Length - 1; i++)
            //    Console.WriteLine("Numeros [" + i + "] = " + numeros[i]);

            //foreach (int n in numeros)
            //    Console.WriteLine(n);

            //    int [,] numeros = new int[3,2] { { 99, 88}, { 12, 34 }, { 100, 56 } }
            //    foreach (int n in numeros)
            //        Console.WriteLine(n);

            //    Console.ReadKey();
            //}
        }
    }
}
